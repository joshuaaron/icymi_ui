<div class="carousel">
  <slot/>
</div>

<style>
  .carousel {
    display: flex;
    flex-direction: column;
    height: 100svh;
    overflow-y: auto;
    scroll-snap-type: y mandatory;
    scrollbar-width: none;
    scroll-behavior: smooth;
    scroll-marker-group: after;
  }

  .carousel::-webkit-scrollbar {
    display: none;
  }

  .carousel::scroll-marker-group {
    position: fixed;
    right: 2rem;
    top: 50%;
    translate: 0 -50%;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    z-index: 100;
    opacity: 0;
    animation: fade-blur-in 0.5s 3.2s forwards var(--ease-one);
  }

  .carousel::scroll-button(*) {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: rgba(100, 100, 100, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: white;
    cursor: pointer;
    transition: all 0.2s;
    display: grid;
    padding: 3px;
    position: fixed;
    font-size: 1.125rem;
  }

  .carousel::scroll-button(*):hover {
    background: color-mix(in srgb, var(--primary-theme) 45%, white 15%);
    border-color: color-mix(in srgb, var(--primary-theme) 45%, white 15%);
  }

  .carousel::scroll-button(*):active {
    border-color: var(--form-control);
    color: var(--form-control);
  }

  .carousel::scroll-button(block-start) {
    content: "↑";
    right: 1.5rem;
    bottom: 4.5rem;
  }

  .carousel::scroll-button(block-end) {
    content: "↓";
    right: 1.5rem;
    bottom: 2rem;
    opacity: 0;
    animation: fade-blur-in 0.5s 3.4s both var(--ease-one);
  }

  .carousel::scroll-button(*):disabled {
    opacity: 0;
    pointer-events: none;
  }
</style>
